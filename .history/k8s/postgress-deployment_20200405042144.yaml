apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgress-deployment
spec:
  selector:
    matchLabels:
      app: postgress
  template:
    metadata:
      labels:
        app: postgress
    spec:
      containers:
        - name: myapp
          image: postgress
          ports:
            - containerPort: <Port>
